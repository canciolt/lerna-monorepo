{
  "name": "@ncl-design-system/assets",
  "version": "0.0.0",
  "main": "dist/global.html",
  "publishConfig": {
    "access": "restricted"
  },
  "directories": {
    "dist": "dist"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "vue-tsc && vite build",
    "build:dev": "vue-tsc && vite build --watch",
    "live": "vite preview --port 8888"
  }
}
