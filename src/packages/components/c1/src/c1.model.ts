export enum Logo {
  black = 'black',
  white = 'white',
  shield = 'shield'
}

export declare interface C1_MODEL {
  componentClass?: string,
  emit?: boolean,
  href?: string,
  title: string,
  logo: Logo
}
