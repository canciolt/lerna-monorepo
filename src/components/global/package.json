{
  "name": "global",
  "version": "0.0.1",
  "main": "dist/global.es.js",
  "publishConfig": {
    "access": "public"
  },
  "directories": {
    "dist": "dist"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build:dev": "vue-tsc && vite build --mode development",
    "build:pro": "vue-tsc && vite build --mode production"
  }
}
