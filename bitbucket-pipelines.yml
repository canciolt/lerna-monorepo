image: node:16.13.0

pipelines:
  pull-requests:
    '**':
      - step:
          name: 'Publish to Chromatic'
          caches:
            - node
          script:
            - npm install
            - npm run build:chromatic
            - npx chromatic --project-token=$CHROMATIC_PROJECT_TOKEN -d storybook-static --allow-console-errors --exit-zero-on-changes --patch-build=$your-branch...main